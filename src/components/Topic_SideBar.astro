---
interface Props {
    courseData : any,
    courseName : string
    courseSlug : string
}

const {courseData, courseName, courseSlug} = Astro.props;
let beginner :any[] = [];
let intermediate :any[] = [];
let advance :any[] = [];
let expert :any[] = [];

if(courseData.length > 0){
beginner = courseData[0].topics.filter((topic :any )=>
topic.difficultyLevel === "Beginner"
)
intermediate = courseData[0].topics.filter((topic :any )=>
topic.difficultyLevel === "Intermediate"
)

advance = courseData[0].topics.filter((topic :any )=>
topic.difficultyLevel === "Advanced"
)

expert = courseData[0].topics.filter((topic :any )=>
topic.difficultyLevel === "Expert"
)
}

---

<!-- Mobile Sidebar Overlay + Drawer (for tablet/mobile) -->
<div id="mobileSidebarOverlay"
    class="mobile-sidebar-overlay fixed inset-0 bg-black/50 z-40 lg:hidden invisible opacity-0 transition-all duration-300">
</div>
<!-- lg:relative lg:h-auto -->
<aside id="mobileSidebar" class="fixed inset-y-0 left-0 z-40 flex h-dvh max-h-dvh w-72 sm:w-80 flex-col bg-white dark:bg-gray-900 border-r border-gray-200 dark:border-gray-800 transform -translate-x-full transition-transform duration-300 ease-out lg:relative lg:inset-y-auto lg:h-auto lg:max-h-none lg:translate-x-0 lg:block lg:w-72 xl:w-80 shrink-0 shadow-xl lg:shadow-none">
    <div
        class="shrink-0 bg-white dark:bg-gray-900 z-10 flex justify-between items-center p-4 border-b border-gray-200 dark:border-gray-800 lg:hidden">
        <a href="/" class="flex items-center gap-2">
            <i class="fa-solid fa-house gradient-text"></i>
            <span class="font-semibold">Home</span>
        </a>
        <button id="closeSidebarBtn"
            class="w-8 h-8 rounded-full bg-gray-100 dark:bg-gray-800 text-gray-600 dark:text-gray-300">
            <i class="fas fa-times"></i>
        </button>
    </div>
    <div class="min-h-0 flex-1 overflow-y-auto sidebar-scroll p-4 pb-8">
        <div class="mb-6">
            <h3
                class="text-xs font-semibold uppercase tracking-wider text-gray-500 dark:text-gray-400 mb-3 flex items-center gap-2">
                <i class="fas fa-compass"></i> Topics
            </h3>
            <div class="space-y-5">
                <!-- BEGINNER -->
                <div class="topic-group">
                    <div class="topic-toggle flex justify-between items-center cursor-pointer py-2 px-1 rounded-md hover:bg-gray-100 dark:hover:bg-gray-800/70 transition"
                        data-target="beginnerDropdown">
                        <div class="flex items-center gap-2">
                            <span class="w-1.5 h-1.5 rounded-full bg-emerald-500"></span>
                            <span class="font-semibold">Beginner</span>
                            <span class="text-xs text-gray-500 dark:text-gray-400">(Fundamentals)</span>
                        </div>
                        <i class="fas fa-chevron-down text-gray-500 text-sm"></i>
                    </div>
                    <div id="beginnerDropdown" class="dropdown-submenu ml-4 mt-1 space-y-1">
                        {
                        (beginner.length > 0) &&
                        beginner.map((topic)=>(
                            <a href={`/courses/${courseSlug}/${topic.slug}`}
                            class="block py-1.5 px-3 text-sm rounded-md hover:bg-gray-100 dark:hover:bg-gray-800"><i
                                class="fas fa-play-circle w-4 mr-2 text-emerald-500"></i>{topic.topicName}</a>
                            ))
                        }
                        
                    </div>
                </div>
                <!-- INTERMEDIATE -->
                <div class="topic-group">
                    <div class="topic-toggle flex justify-between items-center cursor-pointer py-2 px-1 rounded-md hover:bg-gray-100 dark:hover:bg-gray-800/70 transition"
                        data-target="intermediateDropdown">
                        <div class="flex items-center gap-2"><span
                                class="w-1.5 h-1.5 rounded-full bg-blue-500"></span><span
                                class="font-semibold">Intermediate</span><span
                                class="text-xs text-gray-500">(Core Mastery)</span></div>
                        <i class="fas fa-chevron-down text-gray-500 text-sm"></i>
                    </div>
                    <div id="intermediateDropdown" class="dropdown-submenu ml-4 mt-1 space-y-1">
                        {
                        (intermediate.length > 0) &&
                        intermediate.map((topic)=>(
                            <a href={`/courses/${courseSlug}/${topic.slug}`}
                            class="block py-1.5 px-3 text-sm rounded-md hover:bg-gray-100 dark:hover:bg-gray-800"><i
                                class="fas fa-play-circle w-4 mr-2 text-blue-400"></i>{topic.topicName}</a>
                            ))
                        }
                    </div>
                </div>
                <!-- ADVANCED -->
                <div class="topic-group">
                    <div class="topic-toggle flex justify-between items-center cursor-pointer py-2 px-1 rounded-md hover:bg-gray-100 dark:hover:bg-gray-800/70 transition"
                        data-target="advancedDropdown">
                        <div class="flex items-center gap-2"><span
                                class="w-1.5 h-1.5 rounded-full bg-purple-500"></span><span
                                class="font-semibold">Advanced</span><span
                                class="text-xs text-gray-500">(Advanced)</span></div>
                        <i class="fas fa-chevron-down text-gray-500 text-sm"></i>
                    </div>
                    <div id="advancedDropdown" class="dropdown-submenu ml-4 mt-1 space-y-1">
                        {
                        (advance.length > 0) &&
                        advance.map((topic)=>(
                            <a href={`/courses/${courseSlug}/${topic.slug}`}
                            class="block py-1.5 px-3 text-sm rounded-md hover:bg-gray-100 dark:hover:bg-gray-800"><i
                                class="fas fa-play-circle w-4 mr-2 text-purple-400"></i>{topic.topicName}</a>
                            ))
                        }
                    </div>
                </div>
                {expert.length > 0 && (
                    <div class="topic-group">
                        <div class="topic-toggle flex justify-between items-center cursor-pointer py-2 px-1 rounded-md hover:bg-gray-100 dark:hover:bg-gray-800/70 transition"
                            data-target="expertDropdown">
                            <div class="flex items-center gap-2"><span
                                    class="w-1.5 h-1.5 rounded-full bg-red-500"></span><span
                                    class="font-semibold">Expert</span><span
                                    class="text-xs text-gray-500">(Production)</span></div>
                            <i class="fas fa-chevron-down text-gray-500 text-sm"></i>
                        </div>
                        <div id="expertDropdown" class="dropdown-submenu ml-4 mt-1 space-y-1">
                            {
                            expert.map((topic)=>(
                                <a href={`/courses/${courseSlug}/${topic.slug}`}
                                class="block py-1.5 px-3 text-sm rounded-md hover:bg-gray-100 dark:hover:bg-gray-800"><i
                                    class="fas fa-play-circle w-4 mr-2 text-red-400"></i>{topic.topicName}</a>
                                ))
                            }
                        </div>
                    </div>
                )}
                <div class="pt-4 border-t border-gray-200 dark:border-gray-800 mt-2">
                    <div class="text-xs text-gray-500 dark:text-gray-400 px-1"><i
                            class="fas fa-book-open mr-1"></i> Resources</div>
                    <div class="mt-2 space-y-1">
                        <a href={`/courses/${courseSlug}/interview-questions`} class="flex items-center gap-2 text-sm px-3 py-1.5 rounded-md hover:bg-gray-100 dark:hover:bg-gray-800">
                            <i class="fas fa-download"></i>
                            Interview Questions
                        </a>
                        <a href={`/courses/${courseSlug}/quizzes`} class="flex items-center gap-2 text-sm px-3 py-1.5 rounded-md hover:bg-gray-100 dark:hover:bg-gray-800"><i
                            class="fas fa-tasks"></i>
                            Quizzes
                        </a>
                        <a href={`/courses/${courseSlug}/project-ideas`} class="flex items-center gap-2 text-sm px-3 py-1.5 rounded-md hover:bg-gray-100 dark:hover:bg-gray-800"><i
                            class="fas fa-tasks"></i>
                            Project Ideas
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</aside>